.azure/
.env
deploy.outputs.json
*.log
node_modules/
# Squad: ignore runtime state (logs, inbox, sessions)
# Note: .squad/casting/* is identity and MUST be committed to main, not ignored
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
.squad/.cache/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# .NET / Aspire optional integration path build output
bin/
obj/
appsettings.Development.json
appsettings.*.Development.json

# Defense-in-depth: never commit obvious credential material
*.pfx
*.p12
id_rsa
id_ed25519
id_dsa
id_ecdsa
